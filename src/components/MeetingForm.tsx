import React from "react";
import { User } from "models/User";

interface MeetingFormProps {
  authToken: string;
  user: User;
}

export const Login = ({ authToken, user }: MeetingFormProps): JSX.Element => {
  return <></>;
};
